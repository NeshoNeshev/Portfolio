@model Portfolio.Web.ViewModels.Administration.Dashboard.CreatePositionInputModel

@{
    this.ViewData["Title"] = "Create Position";

}
<div class="text-center">
    <h1 class="text-primary">@this.ViewData["Title"]</h1>
</div>
<form method="post" asp-action="CreatePosition">
    <section class="border border-primary">
        <div class="container">
            <div class="row" style="margin-bottom: 20px">

                <div class="col-md-2">
                    <label asp-for="SectorId" class="text-primary"></label>
                    <select asp-for="SectorId" class="form-control">
                        @foreach (var item in Model.SectorDropDown)
                        {
                            <option value="@item.Id">@item.SectorName</option>
                        }
                    </select>
                    <span asp-validation-for="SectorId" class="text-danger"></span>
                </div>
                <div class="col-md-2">

                    <label asp-for="PositionName" class="text-primary"></label>
                    <input asp-for="PositionName" class="form-control" placeholder="Enter position name"/>
                    <span asp-validation-for="PositionName" class="text-danger"></span>
                </div>
                <div class="col-md-2">
                    <label asp-for="PositionPeriod" class="text-primary"></label>
                    <input asp-for="PositionPeriod" class="form-control" placeholder="Enter period"/>
                    <span asp-validation-for="PositionPeriod" class="text-danger"></span>
                </div>
                <div class="col-md-4">
                    <label asp-for="MoreInformation" class="text-primary"></label>
                    <textarea asp-for="MoreInformation" rows="4" class="form-control" placeholder="Enter more information"></textarea>
                    <span asp-validation-for="MoreInformation" class="text-danger"></span>
                </div>
            </div>
        </div>
    </section>
    <br />
    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModalLong">
        Save
    </button>

    <div class="modal fade" id="exampleModalLong" tabindex="-1" role="dialog" aria-labelledby="exampleModalLongTitle" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLongTitle">Do you want to save these changes</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">

                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-primary">Save changes</button>
                </div>
            </div>
        </div>
    </div>
</form>